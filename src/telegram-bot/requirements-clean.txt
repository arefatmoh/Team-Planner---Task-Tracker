# UmraGO Telegram Bot - Clean Dependencies
# Minimal, tested, and compatible versions

# Telegram Bot Framework (stable version)
python-telegram-bot==20.7

# Supabase Python Client (compatible version)
supabase==2.3.0

# Environment variables loader
python-dotenv>=1.0.0

# Note: httpx is automatically installed as a dependency of supabase
# No need to specify it separately

